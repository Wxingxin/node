```js
const app = require("express");
const router = express.Router();

router.use("/add", (req, res, next) => {
  res.send("<h1>hello router</h1>");
});

module.exports = router;
```
router 想一个小的express，嵌套主express中